put barcodes, photos, locations into objects
xtest server-side
add uploading for all
clean up json
x listview review
fix listview refreshs
insert uploading into separate thread rather than blocking UI
x set up service properly as a foreground service

TODO for someone else:
-better camera activity
-new UI?
-look into using mytracks for gps capture